import 'package:flutter/material.dart';

class RequestTypeSelector extends StatelessWidget {
  const RequestTypeSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('RequestTypeSelector'),
    );
  }
}